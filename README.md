# Kafka dataflow pipelines
Repo that hosts pipelines that source data from kafka topics

### Local testing

To run locally, you can run deploy-dataflow-dev.sh and set any extra variables that are set in the GHA file
